# ──────────────────────────────────────────────────────────────
# OVO Backend — Python Dependencies
# Install: pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────

# --- Core API Framework ---
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20          # Required for UploadFile / form data

# --- Configuration ---
pydantic-settings==2.9.1          # Typed .env loading
python-dotenv==1.1.0              # .env file support

# --- Supabase (DB + Storage) ---
supabase==2.15.1                  # Supabase Python client (includes postgrest, storage3)
httpx==0.28.1                     # Async HTTP transport for Supabase

# --- AI / Embeddings / LLM Layer ---
groq==0.25.0                      # Groq API client (Llama 3.3 70B inference)
sentence-transformers==3.4.1      # Local vector embeddings

# --- Audio Analysis ---
librosa==0.11.0                   # BPM detection, key estimation, audio features
soundfile==0.13.1                 # WAV I/O backend for librosa
numpy<2                           # Pinned <2 for PyTorch 2.3 compatibility

# --- Audio Format Conversion ---
pydub==0.25.1                     # Audio format conversion (mp3/flac/ogg → wav)
imageio-ffmpeg==0.6.0             # Bundled ffmpeg binary (no system install needed)
demucs==4.0.1                     # Meta's source separation (stem isolation)

# --- Daemon (install separately: pip install sounddevice torch scipy requests) ---
# sounddevice==0.5.1              # Real-time mic capture
# torch==2.7.0                    # Silero VAD runtime + sentence-transformers backend
# scipy==1.15.2                   # WAV file writing from buffer
# requests                        # HTTP POST to backend
